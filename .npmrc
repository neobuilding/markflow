# Required: the project uses TypeScript 7, but typescript-eslint (v8) only supports TS < 6.1.0.
# Without this, `npm ci` / `npm install` fails the peer-dependency check and cannot install.
# Once a typescript-eslint release adds native TS 7 support, this line can be removed.
legacy-peer-deps=true
