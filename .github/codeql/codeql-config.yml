name: "MarkFlow CodeQL config"

# 只扫源码，跳过依赖、构建产物与测试桩，缩短分析时间、减少误报
paths-ignore:
  - node_modules
  - dist
  - release
  - "**/*.test.ts"
  - "**/*.test.tsx"

# 默认安全套件 + 安全与质量扩展查询（含可维护性/正确性规则）
queries:
  - uses: security-and-quality
